# 澄川 AI 中转站重建记录

## 目标与验收

基于用户提供的初代可用实现，定位升级版防降智导致账号调用失败的问题；构建新的中文中转站，提供可用的接入、密钥、用量、成本与运维流程，在指定测试服务器部署并验证。

| 项目 | 当前状态 | 验收证据 |
| --- | --- | --- |
| 初代与升级版对照 | 已运行复现：保护关通过，开则拨号前失败 | `verification/rebuild/upgrade-regression-reproduction.log` |
| 防降智兼容修复 | v3已实现并部署，回归通过；真实账号待验收 | 新旧失败/成功对照、HTTP/WS、语义丢失负例 |
| 全新首页及用户工作台 | 构建与真实浏览器验证通过 | `docs/BROWSER_VERIFICATION.md` |
| 测试服务器 | 应用、PG、Redis健康，15项服务端检查通过 | `/opt/chengchuan-relay/evidence/` |
| 真实上游账号调用 | 待测试账号 | 服务端真实请求与开启/关闭保护对照 |
| 可复现交付 | 源码、脚本、证据、说明与完整目录已整理 | `澄川新站说明.md`、`DIRECTORY_TREE.txt` |

## 已确认与待证实

- [V] 升级源码在原始请求与 OAuth 合法转换后进行严格逐字段比较；初代无此守卫。字符串 input、Codex 不支持的 max_output_tokens 等均可触发 `MODE1_LOSSY_TRANSFORM`。本次已运行完整 Forward 记录器对照复现。
- [V] 升级源码在开启模式一时把全局 TLS 关闭、插件路由配置当作致命错误。此行为不同于初代标准传输。
- [V] 交接中的历史回环测试没有真实上游账号调用；复制在 `verification/` 下的原始日志属于历史证据，不代表本次运行。
- [V] 2026-09-12 04:07:52 UTC 再次查询测试服务器数据库，`accounts=0`、`active_accounts=0`，应用健康；见[上游验收准备状态](../verification/rebuild/upstream-readiness.json)。缺少导入的上游账号，真实 A/B 仍未执行；此前上游入口返回 HTTP 405 不能证明鉴权、模型输出或质量正常。
- [T] 用户实际百分百失败是否包含其他原因，需测试账号和服务端调用结果确定。
- [T] 不能用 HTTP 200 或稳定身份配置证明模型质量提升，需有控制变量的真实评测。

## 文件与数据边界

原始 `sub2初代` 归档和 `sub2最新` 源码保留；开发在独立的 `sub2新站` 目录。未创建 Git 提交或标签。服务器凭据不写入项目、归档或报告。

## 工具与来源

- 基础工程：<https://github.com/Wei-Shaw/sub2api>，LGPL-3.0，沿用用户源码及许可证；不直接覆盖为上游最新版本。
- 本机 Go 1.27.1：从 go.dev 下载，按官方 JSON 清单 SHA-256 校验；用于复现及编译。
- 前端 pnpm 9.15.9：按现有 lockfile 冻结安装，973 包安装成功；本机默认 pnpm 11 不兼容现有 overrides，未因此更新锁文件。
- Paramiko 5.0.0：SSH/SFTP 通信，密码仅通过进程输入使用，主机密钥保存于工作区工具目录。

## 本次验证摘要

- Go定向联合回归：4包、781项测试/子测试通过，0失败、0跳过，日志 `backend-focused-final.jsonl`。后续小修后的Mode1全组与WSS双轮/池隔离各20次重复通过，日志同目录。不是整个仓库全量测试。
- 前端站点相关68项、保护组件/API14项通过；类型检查、定向ESLint和服务端Vite构建通过。
- 目标4包 `go vet` 通过。
- 服务器15项HTTP/API检查通过；真实浏览器验证首页、登录、工作台、手机抽屉和密钥空态。
- 尚未完成：用户真实上游账号对照、管理员本人协议确认、正式域名/HTTPS配置及用户验收。

最后更新：2026-09-12。源码和测试部署已经可供检查，完整目标仍未完成，不以模拟测试代替真实账号验收。
